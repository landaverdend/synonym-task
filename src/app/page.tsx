'use client';

import { callAPI } from '@/lib/actions';
import { useEffect } from 'react';

export default function Home() {
  return <div className="font-sans flex flex-col items-center justify-center h-screen">Home Page...</div>;
}
